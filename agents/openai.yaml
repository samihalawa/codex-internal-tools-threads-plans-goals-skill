interface:
  display_name: "Codex Internal Tools Threads Plans Goals"
  short_description: "Use Codex app tools to inspect projects and threads, recover prior working approaches, create or steer background tasks, manage plans/goals/titles, and avoid reinventing prior solutions."
  default_prompt: "Use $codex-internal-tools-threads-plans-goals-skill when the user asks to inspect Codex projects or threads, create or steer a background Codex task, recover what worked in prior threads, read the current thread terminal, rename/pin/archive threads, manage plans or goals, schedule Codex follow-ups, or orchestrate multiple Codex threads. Search similar threads before inventing a new route, read terminal/thread evidence directly, use codex_app tools before raw session-log fallbacks, and close with the exact actions taken plus proof read."
