interface:
  display_name: "Codex Internal Tools Threads Plans Goals"
  short_description: "Use the smallest Codex-native operation for tasks, threads, terminals, plans, goals, and automations."
  default_prompt: "Use $codex-internal-tools-threads-plans-goals-skill only when the request explicitly concerns Codex tasks, projects, terminals, plans, goals, automations, or materially depends on a prior Codex task. Prefer the smallest native tool call, recover only enough history for the next action, resolve discoverable IDs internally, avoid duplicate tasks and ceremonial ledgers, preserve current user authority, execute the requested work, and read back mutations before reporting them."
